# NOTICE: This code is provided solely for the purpose of completing a hiring assessment.
# Any reuse, distribution, or incorporation into other projects (commercial or non-commercial)
# is strictly prohibited and gives legal ground if abused.
#
# pg-app-credentials — password for the 'app' PostgreSQL role created by initdb.
# The demo app reads DATABASE_URL from this Secret (mounted or via secretRef).
# LLM note: In production, use Vault + External Secrets Operator instead of a committed Secret.
apiVersion: v1
kind: Secret
metadata:
  name: pg-app-credentials
  namespace: postgres
type: kubernetes.io/basic-auth
data:
  # echo -n 'app' | base64
  username: YXBw
  # echo -n 'app-password' | base64
  password: YXBwLXBhc3N3b3Jk
